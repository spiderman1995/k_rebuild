# 分阶段开发计划

> 原则：**每个阶段有明确的交付物和测试关卡（Gate），测试通过后才能进入下一阶段的开发。**
> 数据基础：本地 36 张 448×448×4 RGBA 分钟K线图（`分钟k线图/`），设计依据见 [README.md](README.md)。

## 阶段总览

| 阶段 | 交付物 | 测试关卡（通过标准） | 状态 |
|------|--------|---------------------|------|
| 0. 环境验证 | 环境检查记录 | torch+CUDA 可用、pytorch_msssim 可导入 | ✅ 已通过 |
| 1. 数据加载 | `src/dataset.py` + `tests/test_dataset.py` | 36 张全部加载为 `(3,448,448)` float32 `[0,1]`，alpha 已去除，像素颜色与原图一致 | ✅ 5/5 通过 |
| 2. CAE 模型 | `src/model_cae.py` + `tests/test_model.py` | 前向 shape 正确：`(B,3,448,448)→z(B,d)→(B,3,448,448)`；反向梯度可达全部参数；latent_dim 可配置 | ✅ 6/6 通过 |
| 3. 损失函数 | `src/losses.py` + `tests/test_losses.py` | 相同图片 → loss≈0 且 SSIM≈1；加噪图片 → loss 显著>0；损失可反向传播 | ✅ 5/5 通过 |
| 4. 训练脚本 | `src/train.py` + `tests/test_train.py` | 冒烟测试：小轮数训练后 MSE 显著低于"纯白图基线"（0.0266）的 50%；checkpoint 可保存且可重新加载 | ✅ 150ep MSE=0.0061 |
| 5. 评估脚本 | `src/evaluate.py` + `tests/test_evaluate.py` | 载入 checkpoint 输出每张图 SSIM/PSNR 指标表 + 原图/重建图并排对比图至 `results/` | ✅ 通过 |
| 6. 过拟合完整验证 | 训练收敛的 checkpoint + `results/` 对比图 | 36 张平均 **PSNR ≥ 30dB 且 SSIM ≥ 0.86**（校准依据见下），肉眼确认K线形态、红绿颜色、成交量柱还原 | ✅ PSNR=33.81dB, SSIM=0.8669 |

## 各阶段细节

### 阶段 0：环境验证 ✅

已确认（2026-08-12）：Python 3.11.9 / torch 2.6.0+cu124 / CUDA 可用（RTX 4050 Laptop, 6GB）/ torchvision 0.21 / pytest 7.4 / pytorch_msssim 已安装。

### 阶段 1：数据加载 `src/dataset.py`

- `KLineDataset`（继承 `torch.utils.data.Dataset`）：读取 PNG → 丢弃 alpha 转 RGB → `[0,1]` float32 张量 `(3,448,448)`；可选反色开关 `invert`（默认关）。
- 测试点：样本数=36；shape/dtype/取值范围；抽查某像素 RGB 值与 PIL 直接读取一致；DataLoader 可按 batch 迭代。

### 阶段 2：CAE 模型 `src/model_cae.py`

- 编码器：6 层 stride=2 卷积 `3×448 → 32×224 → 64×112 → 128×56 → 256×28 → 512×14 → 512×7`，展平后全连接 → `z (latent_dim, 默认256)`。
- 解码器：全连接恢复 `512×7×7`，6 层 `Upsample+Conv`（避免棋盘伪影）对称还原 `3×448×448`，输出 `Sigmoid`。
- 测试点：前向 shape、encode/decode 单独调用、梯度可达全部参数、参数量打印。

### 阶段 3：损失函数 `src/losses.py`

- `CombinedLoss(alpha)`：`alpha·MSE + (1-alpha)·(1-SSIM)`，SSIM 用 `pytorch_msssim`（`data_range=1.0`）。
- 测试点：`loss(x,x)≈0`；`loss(x, 噪声)` 显著大于 0；`backward()` 后梯度非空；alpha=1/0 时退化为纯 MSE/纯 SSIM。

### 阶段 4：训练脚本 `src/train.py`

- Adam(1e-3) + ReduceLROnPlateau，batch=8，支持命令行参数（epochs/latent_dim/alpha/设备），每 epoch 打印损失，保存 `checkpoints/cae_best.pth`（含模型权重+配置）。
- 测试点（冒烟）：小轮数训练后 MSE 显著低于纯白基线的 50%；checkpoint 文件存在且 `torch.load` 后模型可前向。

#### 阶段 4 实验记录（2026-08-12）——损失函数与指标校准

开发中发现组合损失（alpha=0.5）训练 60 epoch 后模型只会输出白图（组合损失 0.0920，
仅略优于"纯白图基线" 0.0965），由此展开系列对照实验，结论：

1. **退化解诊断**：白色背景占 91% 像素，SSIM 对纯白输出已给 0.834 高分，
   SSIM 项梯度在早期把模型引向"只画背景"的退化解。
2. **架构排查**（单图过拟合，800步）：baseline / PixelShuffle / GroupNorm 三种解码器
   SSIM 全部卡在 ~0.87，排除解码器上采样方式问题。
3. **损失对照**（单图过拟合，800步）：纯 MSE 效果最好（MSE=0.00042，视觉上K线形态
   与红绿颜色几乎完美还原）；SSIM/MS-SSIM/BCE 无优势。
   → **训练默认改为纯 MSE（alpha=1.0）**，SSIM 降级为评估指标。
4. **SSIM 指标校准**（关键发现）：此类细线条图上，原图横移 1 像素 SSIM=0.869、
   高斯模糊 σ=1.0 SSIM=0.948——视觉完美的重建也只得 ~0.87，
   **原定 SSIM ≥ 0.95 达标线在此图像域不可达**。
   → 阶段 6 达标线修订为：**平均 PSNR ≥ 30dB + SSIM ≥ 0.86 + 肉眼确认**。
   参照基线：纯白图 MSE=0.0266 / PSNR=15.8dB / SSIM=0.834。

### 阶段 5：评估脚本 `src/evaluate.py`

- 载入 checkpoint，对数据集逐张计算 SSIM/PSNR，输出汇总表（均值±最差），并把"原图|重建图"并排图保存到 `results/`。
- 测试点：指标表行数=36；对比图文件生成；指标值域合法（SSIM∈[0,1]）。

### 阶段 6：过拟合完整验证（流程终验）

- 用 36 张充分训练至收敛，验收标准：**平均 PSNR ≥ 30dB 且 SSIM ≥ 0.86**（阶段4校准后的标准），肉眼确认重建图的K线形态、红绿颜色、成交量柱清晰可辨。
- 此阶段证明"框架有能力把这批图编码进特征并重建"，之后才谈迁移到远程约 207 万张的正式训练。

#### 阶段 6 验收记录（2026-08-12）✅

- 训练配置：latent_dim=256、纯 MSE（alpha=1.0）、batch=8、Adam 1e-3 + ReduceLROnPlateau、800 epoch（RTX 4050 上 914 秒），学习率自动衰减 4 次至 6.25e-05。
- 验收结果：36 张平均 **PSNR=33.81dB（≥30 ✓）、SSIM=0.8669（≥0.86 ✓）**；最差样本 PSNR=32.70dB、SSIM=0.8590。
- 肉眼验收：抽查最差 SSIM 与最差 PSNR 两张的对比图（`results/compare_*.png`），K线形态、红绿颜色、成交量柱（含黑色细峰）均与原图肉眼无差别 ✓。
- 附注：SSIM 均值恰落在"1像素错位"校准线（0.869）附近，符合"内容完整、亚像素级错位"的特征，与视觉观感一致。
- checkpoint：`checkpoints/cae_best.pth`（最优 MSE=0.000421）。
- 全量测试套件：18/18 通过（`python -m pytest tests/ -v`，约 4 分 44 秒）。
- 额外交付：`train.py` 支持 `--resume` 断点续训（checkpoint 含 Adam 动量与调度器状态，实测续训损失平滑衔接），为远程约 207 万张训练做准备。

## 第二期开发计划（2026-08-12 制定）

> 延续同一原则：每阶段有交付物与测试关卡，测试通过才进入下一阶段。

| 阶段 | 交付物 | 测试关卡（通过标准） | 状态 |
|------|--------|---------------------|------|
| 7. 日志系统 | `src/logger.py` + 接入 train/evaluate + `tests/test_logger.py` | 日志同时输出到控制台和 `logs/*.log` 文件；重复初始化不产生重复日志；训练日志包含 epoch/损失/学习率，评估日志包含指标 | ✅ 3/3 通过 |
| 8. 数据划分工具 | `src/split.py` + `tests/test_split.py` | 7:1:2 比例正确；三集合互斥且并集=全量；同种子结果可复现；`KLineDataset` 支持按文件清单构造 | ✅ 6/6 通过 |
| 9. 最小充分维度实验 | `src/experiment_latent.py` + `tests/test_experiment.py` + 实验报告 | 机制测试：小轮数跑通、输出 JSON 结构完整；正式实验：512/256/128/64 四组同协议训练+评估，产出"维度→PSNR/SSIM"对照表与结论 | ✅ 完成（含 32/16 扩展；36 张上 16 维即够，真实拐点须远程验证集测定） |

### 阶段 9 实验记录（2026-08-13）

正式实验（600 epoch/维度，seed=42，结果见 `results/local/latent_experiment.json`）：

| latent 维度 | PSNR (dB) | SSIM | 训练耗时 |
|------------|-----------|------|---------|
| 512 | 33.25 | 0.8660 | 1360s |
| 256 | 33.68 | 0.8665 | 962s |
| 128 | 33.94 | 0.8671 | 821s |
| **64** | **34.05** | **0.8671** | 776s |

- 按判定规则（PSNR≥30dB 且距最优≤1dB 的最小维度）：主实验结论 = 64 维。
- 扩展实验（32/16 维，同协议，结果见 `results/local/latent_exp_ext/latent_experiment.json`）：
  **16 维 PSNR=34.03dB / SSIM=0.8669，与 64 维几乎相同**；32 维 32.92dB 略低，
  属训练随机波动（说明不同 run 间有约 1dB 噪声，远程复核时建议多种子取平均）。
- **最终结论与方法论警示**：36 张图上直到 16 维都找不到质量拐点。原因是过拟合
  场景下解码器可以把全部 36 张图"背"进权重里，latent 只需区分是哪一张
  （log2(36)≈5.2 bit），因此**小样本上测不出真实的"最小充分维度"**。
  本地实验的价值是验证了实验管线与判定规则；**真实拐点必须在远程大数据 +
  验证集上测**（模型背不下几十万张图时，latent 才被迫真正编码图片内容）。
- 过程 bug 修复：汇总日志格式串混入全角 `８`（`%８s`），logging 静默吞掉格式化异常导致表头行丢失且机制测试未察觉；已修复，并在机制测试中增加"关键日志落盘断言"防止同类问题。

### 阶段 7：日志系统 `src/logger.py`

- 统一 logger：`setup_logger()` 输出到控制台 + `logs/项目日期.log` 文件（含时间戳/级别/模块名）；各模块用 `logging.getLogger('kline.xxx')` 子 logger。
- 打日志的位置：训练启动配置、每 N epoch 的损失与学习率、checkpoint 保存与续训事件、异常数据告警（dataset 尺寸校验失败）、评估逐图指标与汇总。
- 测试点：日志文件生成且内容含预期字段；重复调用 `setup_logger` 不重复添加 handler（防止日志翻倍）；子 logger 正常传播。

### 阶段 8：数据划分工具 `src/split.py`

- `split_files(files, ratios=(0.7,0.1,0.2), seed=42)`：随机但可复现地切分文件清单，返回 train/val/test 三个列表；为远程约 207 万张做准备，本地用 36 张验证机制。
- `KLineDataset` 增加可选参数 `files`：直接按给定文件清单构造子集数据集。
- 测试点：比例正确（36 → 25/4/7，2026-08-13 起比例定为 7:1:2）；互斥且完整；同种子两次切分结果一致；子集数据集可正常加载。

### 阶段 9：最小充分维度实验 `src/experiment_latent.py`

- **寻找方法**：控制变量——除 latent_dim ∈ {512, 256, 128, 64} 外，数据/轮数/学习率/种子完全相同，各训练一个模型并评估 PSNR/SSIM，得到"维度→质量"曲线，找拐点。
- **判定规则**：满足 `PSNR ≥ 30dB` 且 `PSNR ≥ 最优维度 - 1dB` 的**最小维度**即"最小充分维度"。
- 产出：`results/<机器标识>/latent_experiment.json`（各维度指标）+ 对照表打印 + 结论；实验结果写回本文档。
- 注意：本地 36 张上的结论是**初筛**（衡量拟合能力）；最终结论需在远程数据的验证集上复核（衡量泛化充分性）。
- 测试点（机制）：用小轮数（如 3 epoch）+ 小维度清单跑通全流程，JSON 结构完整、每维度含 psnr/ssim 字段。

## 第三期开发计划（2026-08-13 制定）：224 数据准备 + 破坏重建方法

> 背景：为引入 ViT 类方法，需要 224×224 输入。两台机器各准备 1 万张：
> **本地**（笔记本 GPU）：从 207 万中**顺序**取 1 万张放入 `分钟k线图/`，2×2 均值降采样到 224×224 存入 `pic_to_224x224/`；
> **远程 203**：从 207 万中**随机**抽 1 万张，同样降采样。
> 两台机器用同一个脚本 `src/prepare_224.py`，只是 `--mode seq / random` 不同。

| 阶段 | 交付物 | 测试关卡（通过标准） | 状态 |
|------|--------|---------------------|------|
| 10. 数据准备工具 | `src/prepare_224.py` + `tests/test_prepare.py` | 顺序/随机两种选取模式正确且随机可复现；输出为 224×224×3；像素值 = 原图 2×2 块均值（数值验证）；本地 36 张实跑通过 | ✅ 4/4 通过，36 张实跑 OK |
| 11a. 破坏变换 + 224 数据支持 | `src/corruptions.py`、`dataset.py` 支持 224 + 测试 | 三种破坏（挖块/遮 patch/去色）shape 与破坏比例正确、同种子可复现；224 图可加载 | ✅ 5/5 通过 |
| 11b. CAE 224 适配 | `model_cae.py` 支持 `input_size=224` + 测试 | 224 前向/反向 shape 正确；448 默认行为不变（回归） | ✅ 9/9 通过 |
| 11c. MAE 模型（ViT-Tiny 从零） | `src/model_mae.py` + 测试 | 前向 `(B,3,224,224)→(B,3,224,224)`，梯度可达全部参数 | ✅ 5/5 通过（约400万参数） |
| 11d. 四组对照实验脚本 | `src/experiment_compare4.py` + `plot_compare4.py` + 机制测试 | 小样本小轮数跑通四组；JSON + loss 曲线图生成 | ✅ 通过；全量回归 47/47 |
| 11e. 正式对照实验 | 1 万张 × 4 方法 × 20 epoch 的 loss 曲线图与结论 | 曲线完整、无发散；结果写回本文档 | ✅ 完成（2026-08-13，结论见下） |

#### 阶段 11e 正式实验结果（2026-08-13，本地机，顺序取样的 1 万张）

数据：`results/local/compare4/compare4.json`；曲线：`results/local/compare4/loss_curves.png`。
划分 7000/1000/2000（7:1:2，seed 42）。第 20 epoch 的损失（MSE）：

| 组 | 方法 | 参数量 | 训练 | 验证 | 测试 | 耗时 |
|----|------|--------|------|------|------|------|
| ① | CAE压缩重建 | 1723万 | 0.00784 | 0.00965 | 0.00948 | 20分 |
| ② | 遮挡修复 | 1723万 | 0.00874 | 0.00983 | 0.00966 | 27分 |
| ③ | MAE(ViT-Tiny从零) | 300万 | 0.01976 | 0.01969 | 0.01944 | 42分 |
| ④ | 去色重彩 | 1723万 | 0.00779 | 0.00977 | 0.00960 | 22分 |

结论：
1. **①②④（CNN 系）曲线健康**：20 epoch 内稳定下降且仍未收敛完，训练/验证差距小（轻微过拟合苗头），验证损失三者接近（0.0096~0.0098）。
2. **③ MAE(ViT) 从零训练明显失败**：验证损失从第 2 epoch 起就平在 ~0.0197（约为其他组的 2 倍），基本没学到东西，且训练曲线在第 12/19 epoch 出现尖峰（不稳定）。**印证了"ViT 从零训练吃数据"的预判**——7000 张 × 20 epoch 远不够。
3. 注意：此为**顺序取样**（单一股票时段集中）的结果；203 随机取样的同款实验跑完后应对照，检验取样方式对泛化的影响。

#### 阶段 11f 组③′ 预训练 ViT 对照结果（2026-08-13）✅

组③′ = 与组③完全相同的破坏方式与数据，仅编码器换成 ImageNet 预训练
ViT-Tiny（timm `vit_tiny_patch16_224.augreg_in21k_ft_in1k`，主干 1/10 学习率微调）。
第 20 epoch：训练 0.00968 / 验证 **0.00974** / 测试 0.00959，耗时 40 分钟。

| 对比项 | ③ 从零 ViT | ③′ 预训练 ViT | CNN 最优（①） |
|--------|-----------|--------------|---------------|
| 验证损失@20ep | 0.01969（未学习） | **0.00974（砍半）** | 0.00965 |
| 曲线形态 | 死平 + 训练尖峰 | 平滑稳定下降，20ep 仍在降 | 下降趋缓 |
| 训练/验证差距 | — | **最小（0.00968/0.00974）** | 偏大（0.00784/0.00965）|

结论：
1. **预训练权重彻底修复了 ViT 的小数据失败**：同预算下验证损失砍半，追平 CNN 三组——"ViT 在小数据上必须用预训练"得到干净的实验证实。
2. **③′ 泛化行为五组最佳**：训练/验证差距几乎为零（CNN 组已有轻微过拟合苗头），且第 20 epoch 下降斜率仍最陡——**延长训练大概率超过 CNN 组**，值得跑 50~100 epoch 验证。
3. 五组曲线同图见 `results/local/compare4/loss_curves.png`；组③′ checkpoint：`checkpoints/compare4/mae_pre.pth`。

#### 阶段 11g 50 epoch 延长实验（2026-08-13）✅ —— ③′ 决定性胜出

①②④③′ 四组同协议延长到 50 epoch（③从零版不再重跑，20ep 曲线留作反面参照）。
验证损失轨迹（@20 → @35 → @50 epoch）：

| 组 | @20 | @35 | @50 | 50ep 训练损失 | 判读 |
|----|-----|-----|-----|--------------|------|
| ① CAE压缩重建 | 0.00971 | 0.01047 | 0.01125 | 0.00494 | **过拟合**：验证损失 ~20ep 见底后持续回升 |
| ② 遮挡修复 | 0.01184 | 0.01104 | 0.01109 | 0.00593 | 平台期+震荡，也已过拟合 |
| ④ 去色重彩 | 0.00970 | 0.01045 | 0.01132 | 0.00488 | 同①，过拟合 |
| **③′ MAE(ViT预训练)** | 0.00974 | 0.00905 | **0.00873** | 0.00863 | **全程平滑下降，训练/验证零差距，50ep 仍未收敛** |

结论：
1. **③′ 决定性胜出**：约第 17~20 epoch 起验证损失越过全部 CNN 组，最终 0.00873 为全场最低且仍在下降；CNN 组的泛化上限约 0.0093~0.0096（15~20ep 处），之后全部转入过拟合。
2. **机理解读**：CNN 组 1723 万参数在 7000 张上快速记忆训练集（训练损失一路降到 0.0049 而验证回升）；③′ 只微调 567 万参数且带 ImageNet 先验，学到的是更通用的表征。
3. **路线决策依据**：在 1 万张量级数据上做特征提取，**预训练 ViT + 破坏重建微调是当前最优配方**；CNN 组若使用应在 ~15-20 epoch 早停。
4. 注意：本轮 CNN 组 checkpoint 为 50ep 末态（已过拟合权重）；③′ 的末态即其最优态。
5. 可选后续：③′ 尚未收敛，可单独续跑到 100 epoch 探其上限（约 2.2 小时）。

### 阶段 11 开发备注

- 去色破坏的真实属性：BT.601 灰度化把红绿通道差从 0.667 压到约 0.081——大幅削弱但**非完全抹除**，模型可部分借助残留亮度差，解读组④结果时需注意。
- **本机环境坑（重要）**：torch 自带的 OpenMP 运行时与 anaconda MKL 的重复加载（OMP Error #15），**同一进程里混用 torch 和 matplotlib 会直接崩溃**。解法：绘图独立成 `src/plot_compare4.py`（不 import torch），由实验脚本用子进程调用。远程机器同为 conda + pip torch 时大概率也有此坑。

### 阶段 11 实验协议（2026-08-13 由用户确定）

- **数据**：只用那 1 万张 2×2 均值降采样的 224×224 图（`pic_to_224x224/`），按 **7:1:2** 划分训练/验证/测试（seed=42），四组共用同一划分。
- **轮数**：每组 **20 epoch**；逐 epoch 记录 **训练/验证/测试三条 loss 曲线**，作图对比（验证/测试的破坏图案固定种子，保证曲线平滑可比）。
- **统一实验设计**：四组的目标一致——"从（被破坏的）输入重建原图"，损失一律纯 MSE，因此四组损失值**可直接横向比较**：
  - ① CAE：输入 = 原图（不破坏，压缩重建基线）
  - ② 遮挡修复：输入 = 随机挖掉若干矩形块（填灰 0.5）的图
  - ③ MAE：输入 = 按 16×16 patch 网格随机遮 75%（填灰）的图，ViT 骨干（SimMIM 风格的全序列输入）
  - ④ 去色重彩：输入 = 灰度化的图（三通道复制）
- **控制变量**：①②④ 用同一 CAE-224 架构（latent 256），只有输入破坏方式不同；③ 用 ViT-Tiny（patch16, dim192, depth6, heads3）**从零训练**——不用预训练权重，保证与其他组同起跑线（timm 预训练留作后续升级）。
- **产出**：`results/<机器标识>/compare4/loss_curves*.png` + `compare4.json`。

### 阶段 10 说明

- 术语：448→224 的操作叫**降采样（下采样，downsampling）**；"每 4 个像素（2×2 块）取平均"即 **BOX/AREA 均值插值**，是整数倍降采样失真最小的方法（阶段 4 前的插值对照实验里 AREA 均值保形态最好）。
- 脚本参数：`--src 源目录 --dst-224 输出目录 [--dst-raw 原图落库目录] --n 10000 --mode seq|random --seed 42`。
- 本地用法：1 万张原图放入 `分钟k线图/` 后执行
  `python src/prepare_224.py --src 分钟k线图 --dst-224 pic_to_224x224 --n 10000 --mode seq`
- 远程 203 用法：
  `python src/prepare_224.py --src E:\k_graphy\分钟k线图 --dst-raw 分钟k线图 --dst-224 pic_to_224x224 --n 10000 --mode random --seed 42`
  （`--dst-raw` 会把选中的 1 万张原图也复制一份，便于对照与后续 448 实验）

### 阶段 11h：统一解码器四组重跑（100 epoch，计划在 203 执行）（2026-08-13 制定）

- **动机**：此前 ViT 组与 CNN 组解码器不同（线性 token 头 vs CAE 解码器），对比混入解码器变量；本阶段把四组解码器统一，使方法间严格可比。
- **统一方案**：新模型 `MAEUnified`（`model_mae.py`）= 预训练 ViT-Tiny 编码器 → token 均值池化 → 线性投影到 256 维 z（显式全局瓶颈，与 CNN 组信息通道等价）→ **CAE 同款解码器**；`build_decoder(init_seed)`（`model_cae.py`）保证 cae/inpaint/color/mae_uni 四组解码器**初始权重逐位一致**（有测试断言）。
- **协议**：方法 `cae,inpaint,color,mae_uni`，100 epoch，其余同阶段 11 协议；输出目录用 `results/203/compare4_unified`（203 执行；与旧系列及本机结果双重隔离）。
- 状态：代码与测试已就绪（统一性测试 + 冒烟通过），**等待在 203 执行**（本地单卡约需 10.5 小时）。
- 203 执行命令见"远程迁移阶段"一节末尾。

## 第四期：SOLID + 简洁原则代码审查（2026-08-14 制定）

> 原则已记入 CLAUDE.md。逐点检查 `src/` 全部模块，每点：检查 → 修复违反项 →
> 相关测试通过 → 才进入下一点；全部完成后跑全量测试套件作为终验。
> 务实基调：SOLID 与简洁原则冲突时简洁优先，不为教条引入过度抽象。

| 检查点 | 检查内容 | 状态 |
|--------|---------|------|
| 12-S 单一职责 | 模块/函数是否只做一件事 | ✅ 4 条修复：run_compare 拆为 build_loaders/train_one_method/merge_and_save_history；evaluate 拆出 save_comparison + save_images 开关；train 拆出 _maybe_resume/_save_ckpt 对偶函数；get_machine_tag 移入 src/machine.py |
| 12-O 开闭原则 | 新增方法是否可不改旧逻辑 | ✅ 1 条修复：make_optimizer 改按参数名前缀（backbone.*）判断分组学习率，废除方法名清单——消除"新增方法漏改清单导致结果静默失真"的风险，专项测试覆盖 |
| 12-L 里氏替换 | 模型类契约一致可互换 | ✅ 2 条修复：MAE 补 latent_dim 别名（三类模型统一属性）；compare4 checkpoint 记录模型真实特征维度而非命令行参数 |
| 12-I 接口隔离 | 函数只收需要的参数 | ✅ 3 条修复：train/evaluate 改显式关键字参数（命令行入口 **vars(parse_args())，程序化调用只传所需，getattr 防御补丁删除）；plot_curves 删除死参数 epochs |
| 12-D 依赖倒置 | 循环只依赖模型契约 | ✅ 审查零发现（run_epoch 只依赖 forward(x)→(x_hat,z) 契约） |
| 12-简 简洁原则 | 无炫技写法、复杂写法有注释 | ✅ 4 条修复：make_loader lambda→def；两处三元表达式副作用→基础 if/else；build_decoder 的 zip 相邻配对补逐行注释 |
| 12-终验 | 全量测试套件通过 | ✅ **53/53 通过**（2026-08-14） |

> 审查方式：六个独立审查视角并行扫描（每原则一个），共产出 14 条候选发现，
> 全部采纳并修复；每检查点修复后跑关卡测试（S:10/10、O:4/4、L:12/12、
> I:11/11）通过后才进下一点。

## 待开发项

### 已调整-1：特征维度统一为 512，共用同一解码器（2026-08-16 更新）

- **当前决策**：项目特征维度固定为真实 512 维；禁止补零扩展到 2048 维，
  不再执行 512/1024/2048 三档对照。
- **维度契约**：重建只能经过 `(B,512)` 的全局向量，不允许 token、特征图
  或 skip connection 绕过瓶颈。固定 192 维的旧 MAE 仅作历史参考。
- **解码器**：正式 compare4 使用适度增强的 GroupNorm 残差上采样解码器；
  legacy 版本仅用于兼容历史 checkpoint。
- **实验设计**（控制变量：解码器同一个）：
  - CAE / 遮挡修复 / 去色重彩直接产生真实 512 维 `z`；
  - 预训练 ViT-Tiny 当前 token 宽度和均值池化结果均为 192 维，必须先调整
    编码器结构，不能用 `Linear(192,512)` 冒充真实 512 维信息容量；
  - 四组的 512 维 `z` 直接进入结构和初始化相同的 residual 解码器；
  - 数据/轮数/学习率/种子与四组对照实验协议一致（1 万张 224 图、7:1:2、纯 MSE）。
- **状态**：512 维接口和 residual 解码器已落地；ViT 真实 512 维编码器调整待定。

## 远程迁移阶段（数据在远程机器，本地无法执行）

> **远程数据位置：`E:\k_graphy\分钟k线图`，共 2,076,107 张（约 207 万）**（2026-08-13 记录）。

1. 从约 207 万张中随机抽样 5,000~20,000 张（覆盖不同股票与涨/跌/震荡阶段），用 `src/split.py` 按 **7:1:2 划分训练/验证/测试集**：训练集拟合模型，验证集调超参与早停，测试集只在最终验收时用一次。
2. 用 `src/experiment_latent.py` 在验证集上复核"最小充分维度"结论。
3. 视需要引入 ViT 编码器方案（方案 C，见 README 第四节）。

### 203 机器环境档案（2026-08-14 实测，来自 check_env.py）

| 项目 | 值 |
|------|-----|
| 主机名 / IP | WIN-INE44988TRQ / 162.105.195.203（教育网） |
| 系统 | Windows Server 2012 R2 |
| CPU / 内存 | 48 核 / 256 GB |
| GPU | **Tesla V100-PCIE 32GB**（远强于本地 4050） |
| **驱动 443.18（关键限制）** | **最高支持 CUDA 10.2 → torch 只能用 1.10.1+cu102**，除非升级驱动（共享服务器慎动） |
| 磁盘 | C: 剩 21.6G；D: 剩 2.7T（代码仓库在 D）；E: 剩 621G（数据在 E） |
| 环境决策 | **复用 trae001 环境**（py3.8 + torch 1.10.1+cu102 已验证可用），只补装 pytorch-msssim / timm==0.9.16 / pytest |
| 代码兼容 | `src/compat.py` 的 load_checkpoint 消化 torch 1.10↔2.x 差异；timm 钉 0.9.16（1.0 系需新 torch API） |

### 203 执行阶段 11h 的完整命令（PowerShell）

```powershell
# 1. 拉代码 + 装依赖（torch 按 203 的 CUDA 版本装）
git clone https://github.com/spiderman1995/k_rebuild.git ; cd k_rebuild
pip install -r requirements.txt

# 2. 环境自检（把 env_report.txt 回传，用于确认 GPU/磁盘/包版本）
python src/check_env.py

# 3. 随机抽 1 万张并降采样（--dst-raw 同时留档原图）
python src/prepare_224.py --src E:\k_graphy\分钟k线图 --dst-raw 分钟k线图 --dst-224 pic_to_224x224 --n 10000 --mode random --seed 42

# 4. 环境变量：机器标识（结果目录隔离）+ HuggingFace 缓存与镜像
[Environment]::SetEnvironmentVariable('KLINE_MACHINE','203','User')  # 永久生效
$env:KLINE_MACHINE='203'
$env:HF_HOME='E:\hf_cache'
$env:HF_ENDPOINT='https://hf-mirror.com'   # 直连 HF 超时才需要

# 5. 统一解码器四组 × 100 epoch（首次运行会自动下载 ViT-Tiny 权重约 22MB）
#    结果自动写入 results/203/compare4_unified，与本机 results/local/ 隔离
python src/experiment_compare4.py --img-dir pic_to_224x224 --epochs 100 --methods cae,inpaint,color,mae_uni --out-dir results/203/compare4_unified --ckpt-dir checkpoints/203/compare4_unified

# 6. 跑完把结果推回仓库（203 走 SSH 可推送）
git add results/203 ; git commit -m "203: 统一解码器四组100epoch结果" ; git push
```

> 备注：若 203 GPU 显存小于 6GB，给第 5 步加 `--batch-size 16`。

### 多机协作防冲突约定（2026-08-14 定）

- **结果按机器分文件夹**：实验脚本默认输出 `results/<机器标识>/...`（标识取环境变量 `KLINE_MACHINE`，未设则用主机名）。本机=`local`，203=`203`，天然不冲突。
- **代码与文档只在本机修改**，203 只负责：拉代码 → 跑实验 → 提交自己的 `results/203/`。
- 两台机器都遵守"**push 前先 pull**"；因为改动的是不同路径的文件，git 会自动合并，不会出现冲突。

## 约定

- 代码注释遵守 README 第八节注释规范（模块 docstring、shape 标注、复杂语法逐行解释）。
- 测试统一用 pytest，命令：`python -m pytest tests/ -v`；单阶段测试：`python -m pytest tests/test_dataset.py -v`。
- 每阶段完成后在上方总览表打 ✅ 并记录测试结果，再开始下一阶段。
