# 项目规范（K线图特征提取与重建）

## 设计原则：SOLID 五大核心原则

所有代码须遵守，代码评审按此逐条检查：

1. **S 单一职责（Single Responsibility）**：一个模块/类/函数只做一件事、只有一个被修改的理由。数据加载、模型、损失、训练、评估、绘图分文件，函数内不混杂多个职责。
2. **O 开闭原则（Open/Closed）**：对扩展开放、对修改关闭。新增一种实验方法/破坏方式时，应通过"新增注册项/新增类"实现，尽量不改动已有函数的内部逻辑。
3. **L 里氏替换（Liskov Substitution）**：子类/同族类可互相替换而不破坏调用方。本项目所有模型（CAE/MAE/MAEUnified）必须保持一致的调用契约：`forward(x) -> (x_hat, z)`、`encode(x) -> z`、`count_parameters()`。
4. **I 接口隔离（Interface Segregation）**：调用方不应被迫依赖用不到的接口。函数参数只收它真正需要的东西，不传整包配置让被调函数自己挑。
5. **D 依赖倒置（Dependency Inversion）**：高层逻辑依赖抽象契约而非具体实现。训练/评估循环只依赖"模型契约"（见 L 条），不依赖某个具体模型类的私有细节。

**务实约束**：SOLID 服务于可维护性，不是教条。在本项目规模下，如果某项抽象
（注册表、工厂、接口基类）带来的间接性超过它省下的修改成本，就不做——
由下面的"简洁原则"最终裁决。

## 简洁原则：不炫技

**不要炫技、不要追求复杂的语言特性。基础的 if、else、循环能解决 99% 的
问题。易读、易维护，永远大于炫技需求。**

- 优先用 if/else、for/while、普通函数；能不用就不用：嵌套推导式、
  多层 lambda、元类、装饰器堆叠、运算符重载、过度的 `**` 解包技巧。
- 确有必要使用不直观写法时（如张量维度变换），必须按注释规范逐行解释
  （见 README 第八节）。
- 代码评审时本条与 SOLID 并列检查；两者冲突时，本条优先。

## 既有约定（详见对应文档）

- 注释规范：README.md 第八节（模块 docstring、shape 标注、复杂语法解释）。
- 开发流程：开发计划.md——每阶段有交付物与测试关卡，测试通过才进入下一阶段；
  测试命令 `python -m pytest tests/ -v`。
- 多机协作：结果按机器分文件夹（`results/<KLINE_MACHINE>/`），代码与文档
  只在本机改，203 只跑实验并提交 `results/203/`；push 前先 pull。
- 本机环境坑：C 盘已满（大文件一律 D 盘，HF_HOME=D:\hf_cache）；
  torch 与 matplotlib 同进程混用会因 OpenMP 冲突崩溃（绘图走子进程，
  见 src/plot_compare4.py）。
